nome = "Eduarda"

# Itera sobre cada caractere da string 'nome' juntamente com o índice correspondente
for i, c in enumerate(nome):
    # Imprime a posição e o caractere na iteração atual
    print("Posição {i}: {c}".format(i=i, c=c))

